import React, { Component } from 'react';
import { render } from 'react-dom';
import { 
  Svg, 
  Arrow,
  Button,
  Text,
  Image,
} from '../../src';

const style = {
  fontFamily: 'monospace'
};

class App extends Component {
  doSomething(e) {
    e.preventDefault();
    alert('cool!');
  }
  render() {
    return (
      <div style={style}>
        <h1>{'RESUITE'}</h1>
        <h3>{'<Svg>'}</h3>
        <Svg width={100} height={100}>
          <circle cx='50' cy='50' r='40' stroke='black' strokeWidth='4' fill='white' />
        </Svg>
        <h3>{'<Arrow />'}</h3>
        <Arrow 
          width={30} 
          height={30} 
          direction={'right'}
          strokeWidth={5}
          rounded />
        <h3>{'<Button />'}</h3>
        <form onSubmit={this.doSomething}>
          <Button 
            text={'I’m a button!'}
            size={'small'}
            rounding={90}
            submit />
        </form>
        <h3>{'<Text>'}</h3>
        <Text tag={'h1'} sm={'h6'} md={'h0'} lg={64}>
          {'This is an h1'}
        </Text>
        <h3>{'<Image>'}</h3>
        <Image 
          src='https://cdn.eso.org/images/large/eso1322a.jpg'
          loadingIndicator={<h3>LOADING</h3>}
          errorNotice={<h3>ERROR</h3>}
          width={'25%'} />
      </div>
    );
  }
}

render(
  <App />,
  document.getElementById('app')
);

